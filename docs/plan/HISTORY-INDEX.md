# History index

One row per `## YYYY-MM-DD` entry in `HISTORY.md`, newest first. Only `scribe`
edits this file, and it writes the row in the same commit as the entry.

`HISTORY.md` grows without bound and is the single largest file a session can
accidentally load. This index exists so `planner` can answer "has this been
built before, and where do I read about it?" without opening it. Rows are taken
from `HISTORY.md`'s own headings rather than re-summarized, so the index can be
checked against its source by eye.

Read a full entry by grepping the exact string in the **Heading** column:

```
grep -n '<heading text>' docs/plan/HISTORY.md
```

Then read that section only. A stale index is worse than none — an entry with
no row is one `planner` cannot find, and will re-plan.

| Date | Task IDs | Summary | Heading (grep this exact string) |
|---|---|---|---|
| 2026-09-17 | 25 | Explainable rejection: `MatchResult.getRejectedCandidates()` carries each vetoed candidate with the rule that vetoed it, closing what D4's drop-the-candidate decision cost. Rules identified by configured position, forced by measurement — every `CandidateRule` is a lambda and a lambda's class name is not stable across builds, so a name-based key would have been a false claim of stability. Additive by copy-with, so `MatchDecisionEngine` was never touched; rejections attach *after* the engine decides, making a veto reaching the decision structurally impossible. A veto and an unscorable result both return null and only the first is a rejection. D12's narrow projection still looks right after a second carrying, and argues for itself. Cost named: two parallel lists that must not overlap, with the invariant in a test rather than the type. 621 tests; the plan now holds one unchecked item, task 28 | 2026-09-17 — Explainable rejection closes milestone 5, and the plan empties but for a gate no code closes (task 25) |
| 2026-09-17 | 30, 31, 32 | Milestone 6, 602 tests. Three of milestone 5's five gaps closed, one narrowed, one corrected — and the two that did not close say so rather than being ticked. `DomainVocabularyTest` reads core's own sources and fails naming file and line; the rule 6 collision resolved by prescribing wording, not exempting the statement. Writing it found the hand-run grep's own bug: a word boundary means `getLastName` never matched, which is part of why the rule went unenforced for six milestones. doclint gates stale `{@link}`s at `show=private` over main and test sources — but not the prose defect that raised the gap, so that gap stays open. Task 32 added no tests: all three `default` methods were already pinned and four mutation probes were all caught by core, which corrects the gap from "covered only in profiles" to the harder problem of missing *combinations*. Three self-corrections: an invalid planning measurement (javadoc run without doclint), a gate that would have missed the motivating defect on both counts, and a proof that was a false positive because another gate fired first | 2026-09-17 — Milestone 6: the rules check themselves, and two of the five gaps turn out to be differently shaped (tasks 30, 31, 32) |
| 2026-09-17 | 26, 27, 29 | Milestone 5 wave 1, 598 tests. Composite rule selectable per group (`SMALLEST | AVERAGE | STRONGEST`), additive via a `default` method; `AVERAGE` broke the scorer's keep-one-member shape, so a carrier now reports `compositeCombined` rather than claiming the group's weight as its own — which is the evidence that the rule 7 amendment earned its place on first use. Core fixtures renamed neutral with D1's asymmetry preserved and no assertion value moved. Alias tiers reordered: exactly two derived-pair expectations moved, all declared edges held, BeatTheJoinTest green before and after. Biggest lesson: core's alias merge tests never mixed translation with nickname, so 31 tests stayed green under a reorder of those very tiers — the behaviour was pinned only in profiles. Both new suites mutation-checked | 2026-09-17 — Milestone 5 wave 1: composite rules per group, neutral core fixtures, reordered alias tiers (tasks 26, 27, 29) |
| 2026-09-17 | decisions | Eight standing questions answered; no code changed. D19 closes — Maven coordinates confirmed, phonetics out of v1, alias-corpus provenance becomes a release gate. D4 amended to match the code: a veto drops the candidate, because `RuleBasedScorer` never inspects `isComplete()` and would let a vetoed candidate score above `matchThreshold`, making a hard veto overridable. Alias ordering reopened and swapped to VARIANT > TRANSLATION > NICKNAME (task 29), over a recorded objection about arbitrary anglicisations; the illustrative-accessor rename was proposed and declined. The composite rule becomes selectable per declared group, default `SMALLEST`. Rule 7 now admits a design artefact. The domain-vocabulary rule now binds `src/test` — and amending it exposed that its bare-`name` grep had never passed on `src/main` either, since core cannot describe a field without naming one; `name` is now banned only in its personal sense, and the remaining 112 hits sit in four `endtoend/` files, measured rather than estimated — dated, not silent. Milestone 5 named: tasks 25–28 | 2026-09-17 — Eight standing questions decided; D19 closes and milestone 5 is named (decisions) |
| 2026-09-17 | 23, 24 | Milestone 4 closes: FS driven through a whole resolver, plus `docs/calibration.md`. Verdict — the probabilistic path's real gain is frequency adjustment (a common agreement can fall below the match threshold where a rare one matches), not the probability, which is arithmetic on unmeasured numbers. The end-to-end test caught a live defect: a corpus covering one field inflated every other to ~19 bits, because the table's floor for an uncovered field is its *rarest* answer; the ordering claim still held under it, so an ordering-only test would have shipped it. Also found the resolver never passes incomplete evidence to a scorer. Rule 7 exception for the doc noted, not passed over | 2026-09-17 — Milestone 4 complete: Fellegi-Sunter ships, and its end-to-end test earns its keep (tasks 23, 24) |
| 2026-09-17 | 20, 21 | `DefaultFellegiSunterModel` and `FellegiSunterScorer`. Task 21's judgement: nothing bent — MatchScorer, ScoringResult and FieldContribution all took a probabilistic scorer unchanged, and ScoringResult's unscorable split (built for D4 in task 06) finally got a second user. Missingness declared not inferred; a composite contributes its smallest member's weight because overconfidence is D11's named failure; prior odds distinguish "nobody said" from "even odds". An acceptance criterion asking for a clamp was amended — it would have guarded nothing | 2026-09-17 — The Fellegi-Sunter model and scorer land (tasks 20, 21) |
| 2026-09-17 | 19, 22 | `TermFrequencyTable` with a floor so an unseen key never yields `u = 0`; `LabelledMatchExample` and `UnlabelledMatchExample`, the latter because `m` is normally estimated by EM over unlabelled pairs and a labelled-only shape blocks that path; `FeatureExtractor` and `ProbabilityModel` shipped as interfaces with no main-source implementation, per D17 | 2026-09-17 — Wave 1 of D5: frequency table, training representation, extension points (tasks 19, 22) |
| 2026-09-17 | 18 | `FieldContribution` carries the subsumption, so a consumer reading `MatchResult` can tell a genuine tie (both candidates contain the source) from a coincidental one; narrow projection chosen over exposing `MatchEvidence` because the evidence carries a frequency key, a prepared value D10 forbids in a logged result; an existing accessor guard caught the addition and now states its criterion rather than listing names; mutation-checked, and a first run that appeared to show weak coverage was the reactor halting before the profiles module | 2026-09-17 — The explanation carries why two candidates tied (task 18) |
| 2026-09-17 | 17 | D6 closed after two milestones: `MatchDecisionEngine.declaredThresholds()` lets `build()` inspect the object that actually decides, rejecting both a wrong-scale engine and one whose thresholds diverge from the declared ones. Exposing only the scale would not have worked — the existing characterization test diverges two instances that are both POINTS — so D6's parenthetical "and ideally its thresholds" was the load-bearing half. Comparison by value because an existing test already passed two equal instances; an engine declaring nothing still builds | 2026-09-17 — D6 closed: a decision engine declares what it applies (task 17) |
| 2026-09-17 | 16 | Milestone 2 closes: `BeatTheJoinTest` lands in profiles-ie and states the thesis as falsifiable — the same records resolve MATCH at 85 with the real pipelines and NO_MATCH at −10 under a generous exact-only control, asserted in one test; §88's two expected bands (alias, VERY_HIGH) are reached where milestone 1 recorded LOW and MEDIUM, and attributed by banding the same pair through the old generic comparator; §90 reviews at margin zero with address SUBSUMED not CONFLICT; passed first run so it was mutation-checked — stripping D7/D9 turns 5 of 11 red; three wave-5 defects closed in the milestone-1 suite; gap found and left open: MatchResult does not carry the subsumption signal | 2026-09-17 — Milestone 2 complete: the library beats a join (task 16) |
| 2026-09-17 | 15 | First real content in `jresolve-profiles-ie`: two deliberately different normalizers, illustrative alias tables, a two-stage address comparator that is task 12's first out-of-module use of the opened extension points; D15's split exercised rather than asserted; three defects found in build, two of them the implementer's own — a variant pair that normalization already closed (rejected by the repository, now a documented guard), two address shapes not converging on a prepared value, and a derived-pair expectation that contradicted the merge rule; D19 provenance still open | 2026-09-17 — Irish profiles land, and the module split stops being an assertion (task 15) |
| 2026-09-17 | 13, 14 | `AliasAwareFieldComparator` (three arms, delegate consulted only on the third, alias hit carries neither similarity nor frequency key) and `TokenSubsumptionComparator` (mints SUBSUMED and PARTIAL_OVERLAP, containment is never CONFLICT, refuses the vacuous empty-set case); acceptance corrected — the flat claim that NOT_APPLICABLE is never returned was false for null combinations and the test only covered non-null pairs; null-safety sweep assigned to 14 alone to avoid two tasks in one file | 2026-09-17 — Alias and subsumption comparators land (tasks 13, 14) |
| 2026-09-17 | 09, 10, 11, 12 | Wave 1, all dependency-free: `DefaultTokenSplitter` (three-wave-old flag closed, code-point iteration); `TokenSubsumption` plus a Java 8 default method keeping `FieldEvidence` additive; `DefaultAliasRepository` storing the alias kind **per pair** via maximum-bottleneck path rather than per group; field-layer extension points opened with an out-of-package `extension` test package as proof. Two corrections to claims rather than code (an inverted comment about reflection, a Javadoc case the test disproved) and two planning amendments (12's Owns insufficient; 11 specified the wrong exception type) | 2026-09-17 — Wave 1: splitter, subsumption signal, alias repository, extension points (tasks 09, 10, 11, 12) |
| 2026-09-17 | kit | SessionStart hook provisions JDK 17 and `toolchains.xml` so `mvn verify` runs in a fresh web container; reads the version from the pom rather than restating it, no-op unless `CLAUDE_CODE_REMOTE=true`, warm-up never fails session start; `.gitignore` narrowed from `/.claude/` to `/.claude/*` with exceptions for `hooks/` and `settings.json`, since a hook only runs if committed | 2026-09-17 — A SessionStart hook provisions the JDK 17 toolchain for web sessions (kit) |
| 2026-09-09 | 08 | `endtoend/` package lands: full-pipeline `EndToEndResolutionTest` proving positive/negative/ambiguous resolves, veto short-circuit, missing-vs-conflict, and determinism under shuffle and ties; two attempts, both code-correct — attempt 1 rejected for documentation only (assertions didn't state §88's expected bands or that two exact fields alone clear the match threshold), fixed with a doc-only diff verified byte-identical elsewhere; closes milestone 1 (all 8 tasks merged, 262 tests, `BUILD SUCCESS`); reviewer's milestone verdict: composition proven, resolution quality not (every positive outcome carried by exact surname + DOB, a plain join would match), D7/D9 are the real gap | 2026-09-09 — Milestone 1 complete: end-to-end resolve proven, gaps recorded (task 08) |
| 2026-09-09 | 07 | `api/` package lands: `EntityResolver`, `EntityResolverBuilder`, `CandidateRule`, wiring `field/`, `scoring/`, `decision/` into cost-tiered compare/veto/score/decide with D6-config validation at `build()`; two attempts, second closed a null-rule and an unenforced-`required` guard but left the D6 scale check documented-only, not enforced (`MatchDecisionEngine` cannot expose its scale from inside this task's `Owns`); union build 251 tests green | 2026-09-09 — Resolver and builder land (task 07) |
| 2026-09-09 | 05, 06 | `field/`, `scoring/` and `decision/` packages land; both tasks passed on first attempt (first wave to do so); union build 229 tests green (142+53+34, no overlap), settling the 175/176 discrepancy in 06's own count; D2 (prepare-once) demonstrated behaviourally via invocation counting; 04's `MatchResult` legal set held against its first real producer, checked by both review and an independent probe | 2026-09-09 — Field layer, scoring and decision land (tasks 05, 06) |
| 2026-09-09 | 03, 04 | `normalization/`, `evidence/` and `result/` packages land; union build 142 tests/21 classes green. Both tasks took 3 attempts, both for defects invisible to a green suite — an invisible-character literal in 03, a self-contradicting `MatchResult` state space in 04, closed only when generalized to a class-level fix (byte enumeration; 24-combination legal-state matrix) | 2026-09-09 — Normalization primitives and core value types land (tasks 03, 04) |
| 2026-09-09 | 02 | `comparison/` package lands: Jaro-Winkler, Levenshtein, token-set similarity metrics, all bounded/reflexive/exactly-symmetric; rejected once for a self-referential test assertion | 2026-09-09 — Similarity metrics land (task 02) |
| 2026-09-09 | 01 | Reactor build stood up: core + profiles-ie modules, JDK 17 toolchain, `release=8`, non-skippable animal-sniffer `java18` gate, JUnit 5 + AssertJ | 2026-09-09 — Maven multi-module build skeleton lands (task 01) |
